# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano//zmk" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano//zmk
#     shield: reviung41
#   - board: nice_nano//zmk
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  # One firmware, two Bluetooth names, so the two Sofles can be told apart when
  # pairing. Only CONFIG_BT_DEVICE_NAME differs: USB and the device model stay
  # SofleL-FlatMT (from config/sofle.conf) on both. Karabiner matches the USB
  # IDs, not either name.
  #
  # New Sofle (serial ...33F97).
  - board: nice_nano//zmk
    shield: sofle_left
    cmake-args: -DCONFIG_BT_DEVICE_NAME=\"lightsofle\"
    artifact-name: lightsofle
  # Old Sofle (serial ...2707E).
  - board: nice_nano//zmk
    shield: sofle_left
    cmake-args: -DCONFIG_BT_DEVICE_NAME=\"darksofle\"
    artifact-name: darksofle
